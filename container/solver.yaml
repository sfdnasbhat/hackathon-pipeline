apiVersion: ai.sap.com/v1alpha1
kind: ServingTemplate
metadata:
  name: test-scenario-serving
  annotations:
    scenarios.ai.sap.com/description: "just a test scenario"
    scenarios.ai.sap.com/name: "testing-cap"
    executables.ai.sap.com/description: "test-scenario-descr"
    executables.ai.sap.com/name: "test-scenario"
    executables.ai.sap.com/id: "test-scenario-serving-v1"
  labels:
    scenarios.ai.sap.com/id: "test-scenario"
    ai.sap.com/version: 0.0.1
    ai.sap.com/resourcePlan: infer.m
spec:
  inputs:
    artifacts:
    - name: model_uri
  imagePullSecrets:
  - name: dckr_pat_iOZpHspmBcRxodGLlYf5__ogR6Y
  template:
    spec: |
      predictor:
        minReplicas: 1
        maxReplicas: 2
        containers:
        - name: kserve-container
          image: "docker.io/sfdnasbhat/hackathon-repo:latest"
          ports:
          - containerPort: 9001
            protocol: TCP
          env:
          - name: STORAGE_URI
            value: "{{inputs.artifacts.model_uri}}"
        imagePullSecrets:
        - name: dckr_pat_iOZpHspmBcRxodGLlYf5__ogR6Y
    apiVersion: serving.kserve.io/v1beta1
